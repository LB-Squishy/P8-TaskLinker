{% extends 'base.html.twig' %}

{% block title %}
    {{tache.titre}} - TaskLinker
{% endblock %}

{% block header_content %}
    <div class="flex-header">
        <h1>{{tache.titre}}</h1>
    </div>
{% endblock %}

{% block body %}
    <div id="content">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div>
            <br>
            <button type="submit" class="button button-submit">Modifier</button>
            <a href="{{ path('app_tache_delete', {'tacheId': tache.id}) }}" class="delete-task">Supprimer la tâche</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
